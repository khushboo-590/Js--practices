



// An array is a variable that stores multiple values of the same type.Each value in an array is called an element.


let numbers = [10, 30, 40, 60, 80]

console.log(numbers[0]);  

console.log(numbers[2]);  
